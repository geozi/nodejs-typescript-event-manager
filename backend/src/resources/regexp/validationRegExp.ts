export const PHONE_NUMBER_REGEX = new RegExp(/^\d{10}$/);

export const CITY_REGEX = new RegExp(/^[A-Za-z\s]+$/);

// EMP -DATE(YYY/MM/DD) - RN - 6 DIGITS
export const TICKET_ID_REGEX = new RegExp(/^EMP\d{8}RN\d{6}$/);
